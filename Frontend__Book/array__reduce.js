/* concept:

can do aggregation , segregation , running things in series/sequence.


Syntax :
 */

//aggregation
/* const arr=[1,2,3];
const sum=arr.reduce((prevValue,curValue,curIndex,array)=>{
    return prevValue+curValue;
},0);
console.log(sum);

//segregation
const segregate=arr. */